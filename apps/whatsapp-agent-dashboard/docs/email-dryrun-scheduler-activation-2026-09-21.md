# Email DRY_RUN scheduler activation checkpoint

This marker exists only to route the guarded Email DRY_RUN scheduler activation change through the repository's required CI checks. It changes no application runtime behavior.
