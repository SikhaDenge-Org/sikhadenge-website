import { applyEmailMarketingUnsubscribe } from "@/modules/email-automation/campaigns/unsubscribe";
export const runtime="nodejs"; export const dynamic="force-dynamic";
function page(title:string,body:string,status=200){return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${title}</title></head><body style="font-family:Arial,sans-serif;max-width:640px;margin:60px auto;padding:24px"><h1>${title}</h1><p>${body}</p></body></html>`,{status,headers:{"content-type":"text/html; charset=utf-8","cache-control":"no-store"}});}
export async function GET(request:Request){try{const token=new URL(request.url).searchParams.get("t")||"";if(!token)return page("Invalid unsubscribe link","The unsubscribe token is missing.",400);await applyEmailMarketingUnsubscribe(token);return page("Email preferences updated","You have been unsubscribed from marketing emails for this workspace.");}catch(e){return page("Unsubscribe failed",e instanceof Error?e.message:"The unsubscribe request could not be completed.",400);}}