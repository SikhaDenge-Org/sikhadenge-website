#!/usr/bin/env bash
# Deployment marker for the validated Email workspace split.
# Retry marker: 2026-09-16 after a pre-job workflow cancellation.
# Intentionally no-op; its path is within the guarded production-batch trigger allowlist.
exit 0
