#!/usr/bin/env bash
# Phase17 exact-SHA Production Batch trigger marker.
# Intentionally not invoked by production runtime; this file exists only so the guarded
# path-filtered Production Batch workflow can receive a push event for exact-SHA evidence.
exit 0
